🧠 v3.4.1 — Control Panel (UI Toggle + Token Tracker)

JoJo can now switch Mock Mode in real time from the UI and report live token usage and approximate API costs. All toggles and usage deltas are logged in logs/ui/usage.log. The system status API exposes mock_mode, tokens, and usd for live dashboards.

