# v1.6-stress-tested — Milestone Report

- Version: v1.6-stress-tested
- Release date: ${DATE}
- Stage: stable-core-polished (pre-Phase 2)

## Summary

- Core tests: 40 passed (0 failed)
- Stress builds: 21 executed, 0 failures (100% success)
- Logs: Aggregated to `logs/stress_summary.json` with durations and success rate

## Supported Templates

- Web
  - `fastapi_full_app` — Multi-route FastAPI + Jinja2 + SQLite (/, /log, /history, /summary)
  - `fastapi_dashboard` — Dashboard + home + health routes
  - `flask_minimal` — Minimal FastAPI-compatible stub mimicking Flask simplicity
  - `imagegen_fastapi` — `/generate` endpoint producing an artifact; plus `/`, `/health`, `/history`, `/summary`
  - `streamlit_basic` — Streamlit scaffold (marked as not applicable for web evaluator)
- CLI
  - `automation_cli` — File organizer CLI
- ML
  - `ml_basic` — Starter notebook scaffold (existing in `templates/ml`)

## Builders and Evaluation

- Web Builder
  - Auto-detects keywords and selects matching templates: “dashboard”, “flask”, “streamlit”, “image/prompt”.
  - Falls back to dynamic or static web templates; full app uses `fastapi_full_app`.
  - Evaluator: ASGI evaluation for FastAPI apps; skips Streamlit with `status: template_not_applicable`.

- CLI Builder
  - Default CLI scaffold; if keywords include “automation/organize”, uses `automation_cli`.
  - Evaluator: Detects Python CLI entry and argparse presence.

- ML Builder
  - Provides `starter.ipynb`; evaluator validates basic notebook structure.

## Operational Notes

- Auto Port Recovery: Startup selects a free port in 8015–8050; logs to `logs/startup.log`.
- Quick Launch: `python -m uvicorn ai_factory.main:app --reload --port 8015` or run the app directly; `scripts/run_app.py` helps launch generated apps.
- Evaluator Skip Logic: Streamlit-based apps and unknown domains are marked `template_not_applicable` and treated as pass/skipped.
- Dependencies: `python-multipart` installed to support FastAPI forms (used in `fastapi_full_app`).

## Stress Testing Artifacts

- JSONL log: `logs/stress_test.log`
- Summary: `logs/stress_summary.json`

## Next Phase

- Phase 2: Orchestrator + Supervisor integration (MCP routes and adaptive strategies). The system is prepared with stable core, enhanced templates, and robust evaluator logging/skips.

