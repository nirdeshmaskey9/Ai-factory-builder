# {{ app_name }} — Web Scaffold

This is a static landing page generated by AI Factory.

## Getting Started

Open `index.html` in your browser.

## Customize

- Edit the `<title>`, `<h1>`, and description in `index.html`.
- Add sections inside the `.features` cards.

## Provenance

- Build artifacts are stored under `builds/<build_id>/outputs/{{ app_name }}/`.
- The build manifest at `builds/<build_id>/manifest.json` records version and timestamps.
